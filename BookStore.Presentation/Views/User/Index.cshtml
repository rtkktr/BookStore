@using Microsoft.AspNetCore.Identity;
@model List<IdentityUser>

<div class="container mt-4">
    <h2>User List</h2>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>UserName</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Count == 0)
            {
                <tr>
                    <td colspan="4">There are no registered users in the app</td>
                </tr>
            }
            else
            {
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Email</td>
                        <td>@item.UserName</td>
                        <td>
                            <a asp-action="EditUserRoles" asp-controller="User" asp-route-id="@item.Id" class="btn btn-info btn-sm">Edit User</a>
                            <a asp-action="Delete" asp-controller="User" asp-route-id="@item.Id" class="btn btn-danger btn-sm ml-2">Delete</a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>